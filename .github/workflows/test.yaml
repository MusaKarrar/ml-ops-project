name: test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Run a simple script
      run: |
        echo "Hello, world!"
        echo "This is a basic CI workflow."
